### Files
The following are the main files in this folder:

1. Arima_sampling_10s_pred_1m.ipynb (sashaikh)
	The jupyter notebook implementation of our integrated module. 
	You can run this on offline data sets ".csv" in this directory (eval) to evaluate the algorithms.

2. HW_sampling_10s_pred_1m.ipynb (sashaikh)
	The jupyter notebook implementation of our integrated module. 
	You can run this on offline data sets ".csv" in this directory (eval) to evaluate the algorithms.

3. wma_sampling_10s_pred_1m.ipynb (sashaikh)
	The jupyter notebook implementation of our integrated module. 
	You can run this on offline data sets ".csv" in this directory (eval) to evaluate the algorithms.
